pipeline {
     agent any
     stages {
          stage('Checkout') {
               steps {
                    git url: 'https://github.com/aludom1730/calculator'
               }
          }
          stage('Compile') {
               steps {
                    echo 'to do'
               }
          }
     }
}
